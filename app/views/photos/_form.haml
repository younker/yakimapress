= form_for [@product, Photo.new], :html => { :multipart => true } do |f|

  .field
    = f.label :photo
    = f.file_field :photo, :size => 35

  .actions.clearfix
    = f.submit 'Add Product Photo', :class => 'button'

